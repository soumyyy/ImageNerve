# Copy to .env and fill in values. Required for full functionality.

# PostgreSQL (Supabase or any Postgres). Required for photos, albums, faces, etc.
# Must include the password. Get it from Supabase: Project Settings → Database → Connection string (URI).
DATABASE_URL=postgresql://user:YOUR_PASSWORD@host:5432/dbname?sslmode=require
# Supabase direct connection (replace YOUR_PASSWORD and your project host):
# SUPABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.xxxx.supabase.co:5432/postgres?sslmode=require

# AWS S3 (for photo uploads/downloads)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET_NAME=your-bucket-name

# Optional
LOG_LEVEL=INFO
